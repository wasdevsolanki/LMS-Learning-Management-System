<?php require_once("require/header.php"); ?>

<div class="row">
	<div class="col-sm-12 text-center p-3 ">
		<h1 class="display-5 p-3">Training Programs</h1>
		<center><hr class="w-50">
		<span class="fs-5">Select Course</span>
		<select class="form-control w-25">
			<option>PHP Basic</option>
			<option>PHP Advance</option>
			<option>Java Basic</option>
			<option>Java Advance</option>
			<option>Networks Basic</option>
			<option>Network Advance</option>
		</select>
		</center>
	</div>
</div>

<div class="row">
	<div class="col-sm-2"></div>
	<div class="col-sm-8">
		<div class="card">
		  <div class="card-body">
		    <h5 class="card-title">PHP Basic</h5>
		    <hr>
		    <p class="card-text">
		    	
		    	Stands for "Hypertext Preprocessor." (It is a recursive acronym, if you can understand what that means.) PHP is an HTML-embedded Web scripting language. This means PHP code can be inserted into the HTML of a Web page. When a PHP page is accessed, the PHP code is read or "parsed" by the server the page resides on. The output from the PHP functions on the page are typically returned as HTML code, which can be read by the browser. Because the PHP code is transformed into HTML before the page is loaded, users cannot view the PHP code on a page. This make PHP pages secure enough to access databases and other secure information.
		    	The output from the PHP functions on the page are typically returned as HTML code, which can be read by the browser. Because the PHP code is transformed into HTML before the page is loaded, users cannot view the PHP code on a page. This make PHP pages secure enough to access databases and other secure information.The output from the PHP functions on the page are typically returned as HTML code, which can be read by the browser. Because the PHP code is transformed into HTML before the page is loaded, users cannot view the PHP code on a page. This make PHP pages secure enough to access databases and other 
				A lot of the syntax of PHP is borrowed from other languages such as C, Java and Perl. However, PHP has a number of unique features and specific functions as well. The goal of the language is to allow Web developers to write dynamically generated pages quickly and easily. database-driven Web sites. If you would like to learn more about PHP, the official site is PHP.net.
		    </p>

		        <a href="#" class="btn float-end text-light rounded-pill" style="background-color:#2B7A77;">more details...</a>
		  </div>
		</div>

	</div>
	<div class="col-sm-2"></div>


<?php require_once("require/footer.php"); ?>